name: Temporary V9 branch CI
on:
  push:
    branches:
      - feature/v9-shadow-production
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run lint && npm test && npm run build
