name: Temporary pressure cleanup CI
on:
  push:
    branches:
      - cleanup/remove-pressure-calculator-artifacts
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run lint && npm test && npm run build
